package ${packageName}.${fragmentPackage}

import ${packageName}.${fragmentPackage}.model.${uiStateName}
import toothpick.InjectConstructor

@InjectConstructor
internal class ${uiConverterName}() {

    fun convert(state: ${mviStateName}): ${uiStateName} {
        return TODO()
    }

}
